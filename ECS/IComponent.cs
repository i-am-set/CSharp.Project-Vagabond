namespace ProjectVagabond
{
    /// <summary>
    /// A marker interface for all components. This is used to enforce a type constraint,
    /// ensuring that only valid component classes can be used within the ComponentStore.
    /// </summary>
    public interface IComponent
    {
    }
}