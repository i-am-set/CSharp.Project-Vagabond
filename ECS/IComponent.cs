namespace ProjectVagabond
{
    /// <summary>
    /// enforces a type constraint, ensuring that only valid component classes can be used within the ComponentStore.
    /// </summary>
    public interface IComponent
    {
    }
}